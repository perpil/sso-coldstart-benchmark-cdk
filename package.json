{
  "name": "coldstart-benchmark",
  "version": "0.1.0",
  "bin": {
    "coldstart-benchmark": "bin/coldstart-benchmark.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "removeSSO": "npx patch-package",
    "addSSO": "npx patch-package --reverse"
  },
  "devDependencies": {
    "aws-cdk": "^2.108.1",
    "aws-cdk-lib": "^2.108.1",
    "esbuild": "^0.19.7",
    "jest": "^29.7.0",
    "patch-package": "^8.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-sts": "3.502.0",
    "constructs": "^10.0.0"
  }
}

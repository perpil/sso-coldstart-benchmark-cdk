{
  "name": "coldstart-benchmark",
  "version": "0.1.0",
  "bin": {
    "coldstart-benchmark": "bin/coldstart-benchmark.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk"
  },
  "devDependencies": {
    "aws-cdk": "2.108.1",
    "aws-cdk-lib": "2.108.1",
    "esbuild": "^0.19.7",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^1.16.0",
    "@aws-lambda-powertools/metrics": "^1.16.0",
    "@aws-lambda-powertools/tracer": "^1.16.0",
    "@aws-sdk/client-sts": "^3.454.0",
    "constructs": "^10.0.0"
  }
}
